---
title: "Overfitting et underfitting : diagnostiquer et corriger"
description: "Pourquoi ton modèle échoue : trop simple ou trop mémorisé ? Comment reconnaître et fixer chaque cas."
---

## Introduction : Deux façons d'échouer

Ton modèle peut échouer de **deux façons complètement opposées**.

**Underfitting** : le modèle est trop simple pour les données. Il ne capture pas les patterns vrais. Erreur d'entraînement **élevée**.

**Overfitting** : le modèle est trop complexe. Il mémorise le bruit et les spécificités de l'entraînement. Erreur d'entraînement **basse** mais erreur de validation **très haute**.

<ImageWithSource
  src="/images/admin/img-1774007963495.png"
  alt="Exemple visuel de surapprentissage (overfitting)"
  source="https://www.researchgate.net/figure/Overfitting-and-underfitting-of-ANN-models-Geron-2019_fig2_371793453"
  caption="Cette image illustre le phénomène de surapprentissage (overfitting) où le modèle, trop complexe, s'adapte parfaitement aux données d'entraînement (en rouge) mais échoue à généraliser (en bleu). La courbe rouge suit de trop près les points d'entraînement, incluant leur bruit."
  size="large"
/>

C'est le compromis **biais-variance** : la fondation de tout apprentissage statistique.

## Biais vs Variance

<ImageWithSource
  src="/images/admin/img-1774008031084.png"
  alt="Diagramme biais-variance en quadrants"
  source="https://www.researchgate.net/figure/Le-compromis-entre-le-biais-et-la-variance_fig2_323141508"
  caption="Ce diagramme illustre les quatre combinaisons possibles entre biais et variance dans un modèle d'apprentissage automatique. Les points noirs représentent les données d'entraînement, tandis que les cercles concentriques montrent la capacité du modèle à s'adapter aux données."
  size="large"
/>

### Biais

C'est l'**erreur systématique** du modèle. Il fait des hypothèses trop simplistes et s'y tient.

Exemple : tu prédis que tous les appartements parisiens coûtent 400k euros (ta moyenne), peu importe la surface. C'est biaisé : tu rates systématiquement.

Cause : modèle trop simple pour les données.

### Variance

**Sensibilité aux petites variations** dans les données. Le modèle change drastiquement si tu changes un peu les données.

Exemple : un arbre de décision très profond apprend les bruits individuels. Change une donnée d'entraînement = arbre complètement différent.

Cause : modèle trop complexe, surapprend.

### La décomposition d'erreur

$$\text{Erreur totale} = \text{Biais}^2 + \text{Variance} + \text{Bruit irréductible}$$

Idéal : petit biais + petite variance.

Underfitting : gros biais + petite variance (systématiquement faux mais stable).

Overfitting : petit biais + grosse variance (adapté à l'entraînement mais imprévisible).

<Callout type="info" title="Analogie du tireur à la cible">
Gros biais, petite variance = tous les tirs à gauche (groupés mais mal placés).
Petit biais, grosse variance = tirs éparpillés autour de la cible.
Idéal = tous les tirs au centre.
</Callout>

## Reconnaître l'underfitting

**Signes** :
- Erreur d'entraînement élevée. Le modèle n'arrive même pas à bien apprendre ses données.
- Erreur de validation similaire à l'erreur d'entraînement. Pas d'écart.
- Mauvaises performances globales. 50% d'accuracy sur une classification à 2 classes ?

**Solutions** :
- Augmente la complexité : plus de couches, plus de neurones
- Ajoute des features plus riches
- Réduis la régularisation (les "freins" sur la complexité)
- Entraîne plus longtemps
- Essaie une meilleure architecture

## Reconnaître l'overfitting

**Signes** :
- Grand écart entre erreur d'entraînement et validation. Train = 2%, Validation = 15%.
- Erreur de validation qui remonte au fil des epochs. Au début elle descend, puis monte.
- Parfait en labo, nul en production. Tes chiffres sont excellents, mais l'utilisateur voit un modèle qui ne fonctionne pas.

<Callout type="warning" title={"Un peu d'overfitting est normal et souhaitable"}>
Zéro overfitting = probablement underfitting. Tu veux un **petit** overfitting : assez complexe pour les vrais patterns, pas trop pour le bruit. C'est un **art**, pas une science.
</Callout>

## Les courbes d'apprentissage : ton meilleur outil de diagnostic

Trace deux courbes : erreur d'entraînement et erreur de validation au fil des epochs.

### Underfitting

Les deux courbes restent hautes et proches. Ajouter plus de données ? Ça n'aide pas (il n'y a pas assez de capacité pour apprendre).

### Overfitting

Erreur train descend (le modèle apprend). Erreur validation remonte (surapprend du bruit). Écart croissant = signal classique.

### Idéal

Les deux courbes descendent ensemble et se stabilisent bas, avec un petit écart (0.5-5% max).

<ImageWithSource
  src="/images/admin/img-1774008117562.png"
  alt="Courbes d'erreur en fonction de la complexité du modèle"
  source="https://aiml.com/what-is-overfitting/"
  caption="Ce graphique illustre l'évolution des erreurs d'entraînement et de test en fonction de la complexité du modèle. Dans la zone idéale, les deux erreurs sont faibles et proches, indiquant un bon équilibre."
  size="large"
/>

## Train/Validation/Test : les trois ensembles sacrés

Jamais entraîner et évaluer sur les mêmes données. C'est de la triche.

<ImageWithSource
  src="/images/admin/img-1774008183135.png"
  alt="Séparation des données en ensembles d'entraînement, validation et test"
  caption="Cette image illustre la nécessité de diviser les données disponibles en trois ensembles distincts : l'ensemble d'entraînement pour apprendre le modèle, l'ensemble de validation pour ajuster les hyperparamètres, et l'ensemble de test pour évaluer les performances finales. Cela évite le biais d'évaluation et le surapprentissage (overfitting)."
  size="large"
/>

### Training Set (70-80%)

Données sur lesquelles le modèle apprend. Poids sont ajustés sur ces données.

### Validation Set (10-15%)

Données que tu regardes **pendant** le développement. Sert à :
- Choisir les hyperparamètres
- Sélectionner l'architecture
- Décider quand arrêter l'entraînement (early stopping)

Le modèle ne s'entraîne pas dessus, tu les **observes** juste.

### Test Set (10-15%)

Données que tu **ne touches qu'une seule fois** à la fin. C'est ton estimé honnête de vraie performance. Sacré. Jamais pour choisir des hyperparamètres.

<Callout type="warning" title="Pourquoi trois et pas deux ?">
Si tu utilises le même ensemble pour choisir les hyperparamètres ET estimer la performance, tu vas biaiser optimiste. Tu finis par choisir le modèle qui gagne sur *ce* ensemble, pas le meilleur. Le test set intact donne une estimation honnête.
</Callout>

## Cross-validation : utiliser 100% des données

Quand tu as peu de données (< 10k), les splits fixes gaspillent. Cross-validation utilise tout.

**k-fold cross-validation** :
1. Divise en k parts (5 ou 10 typiquement)
2. Pour chaque part i : entraîne sur les k-1 autres, évalue sur i
3. Performance finale = moyenne des k évaluations

Exemple : 5-fold
| Fold | Entraînement | Évaluation |
|------|-------------|------------|
| 1 | Parts 2-5 | Part 1 |
| 2 | Parts 1, 3-5 | Part 2 |
| 3 | Parts 1-2, 4-5 | Part 3 |
| 4 | Parts 1-3, 5 | Part 4 |
| 5 | Parts 1-4 | Part 5 |

Performance = moyenne des 5 résultats

Avantage : chaque donnée est utilisée pour apprendre (k-1 fois) et pour l'évaluation (1 fois).

Inconvénient : k fois plus lent.

## Solutions pratiques contre l'overfitting

### 1. Plus de données

La meilleure solution si tu peux. Plus de diversité rend la mémorisation impossible.

### 2. Data Augmentation

Crée des variantes de tes données sans réellement créer de nouvelles données vraies.

**Images** : rotation, flip horizontal, zoom, recadrage aléatoire, bruits, changements de luminosité.

**Texte** : paraphrase, substitution de synonymes, traduction puis retraduction inverse (pour garder le sens tout en variant la formulation).

**Tableaux** : ajoute du bruit gaussien, utilise SMOTE (Synthetic Minority Oversampling Technique) pour surreprésenter les classes rares.

Augmentation intelligente (RandAugment, AutoAugment) choisit automatiquement les meilleures augmentations.

<ImageWithSource
  src="/images/admin/img-1774008251457.png"
  alt="Exemple d'augmentation d'images pour l'IA"
  source="https://www.ibm.com/fr-fr/think/topics/data-augmentation"
  caption="Cette image illustre différentes techniques d'augmentation de données appliquées à une image originale de chat. Ces transformations permettent d'enrichir un jeu de données pour améliorer les performances des modèles d'intelligence artificielle."
  size="large"
/>

### 3. Régularisation

Ajoute des pénalités qui forcent le modèle à rester simple.

**L2 (weight decay)** : pénalise les gros poids.

**Dropout** : désactive aléatoirement des neurones.

<ImageWithSource
  src="/images/admin/img-1774008306012.png"
  alt="Comparaison d'un réseau de neurones standard et avec dropout"
  source="https://medium.com/@amarbudhiraja/https-medium-com-amarbudhiraja-learning-less-to-learn-better-dropout-in-deep-machine-learning-74334da4bfc5"
  caption="L'image montre un réseau de neurones standard (à gauche) et le même réseau après application de la technique de dropout (à droite), où certains neurones sont désactivés aléatoirement pour éviter le surapprentissage."
  size="large"
/>

**Early stopping** : arrête avant que le modèle commence à surapprendre.

(Détails dans la section suivante.)

### 4. Early Stopping

Super simple et super efficace. Surveille l'erreur de validation pendant l'entraînement. Dès qu'elle arrête de descendre (ou commence à remonter), arrête et garde les meilleurs poids.

- **Epoch 1-10** : erreur validation baisse (garde les poids)
- **Epoch 11-15** : erreur validation remonte (stop, utilise poids epoch 10)

Zéro surcoût computationnel. Fait presque toujours du bien.

<ImageWithSource
  src="/images/admin/img-1774008385282.png"
  alt="Courbe de l'erreur de généralisation en fonction de la capacité"
  caption="As a model begins training, highly biased and low in complexity, both the training error and validation error will decrease.  This can be seen in the underfitting zone when looking at the bias and generalization.  If the model trains for long enough the a very complex and unbiased model can be learned but the variance or error increases as seen in the overfitting zone.  The validation set is used in conjunction with early stopping to determine the optimal training zone for a model."
  size="large"
/>

### 5. Réduire la complexité

Moins de couches, moins de neurones, modèle plus simple.

Attention : trop simple = underfitting.

### 6. Ensembles

Combine plusieurs modèles, par voting (vote majoritaire) ou stacking (empilement). Ça réduit la variance automatiquement.

Exemple : 10 arbres de décision plutôt qu'1. La variation aléatoire de l'un est annulée par les autres.

<ImageWithSource
  src="/images/admin/img-1774008478930.png"
  alt="Schéma du processus d'ensemble learning avec 3 apprenants"
  source="https://www.geeksforgeeks.org/machine-learning/ensemble-classifier-data-mining/"
  caption="L'image illustre le principe de l'ensemble learning où plusieurs modèles (learners) sont entraînés sur des échantillons bootstrapés du jeu de données. Leurs prédictions individuelles sont combinées pour former une prédiction finale plus robuste, réduisant ainsi l'impact de la variation aléatoire de chaque modèle."
  size="large"
/>

## La double descente : contre-intuitif

Recherche récente : les très gros modèles (sur-paramétrés) peuvent généraliser **mieux** que des modèles de taille intermédiaire. C'est le phénomène de "double descente" :

L'erreur augmente puis diminue à nouveau. Ce phénomène explique pourquoi les LLM (Large Language Models) géants avec des milliards de paramètres fonctionnent si bien malgré leur énorme sur-paramétrisation.

<ImageWithSource
  src="/images/admin/img-1774008559102.png"
  alt="Courbe d'erreur d'entraînement et de test"
  source="https://www.researchgate.net/figure/The-double-descent-phenomenon-for-highly-overparametrized-networks_fig1_346651475"
  caption="Cette image illustre le phénomène d'overfitting où l'erreur d'entraînement diminue mais l'erreur de test augmente après un certain seuil d'interpolation. Cela montre pourquoi les modèles très grands (comme les GPT) peuvent performer bien malgré leur sur-paramétrisation."
  size="large"
/>

## Checklist diagnostique rapide

1. Trace erreur train vs erreur validation
2. Écart train/validation grand ? -> **Overfitting**. Régularise, ajoute des données, réduis la complexité
3. Erreur train élevée ? -> **Underfitting**. Augmente capacité, entraîne plus
4. Les deux basses et proches ? -> **Bon ajustement**. Valide sur test set
5. Vérifie tes données : erreurs d'étiquetage, outliers bizarres créent un plafond d'erreur

Rapidement diagnostiquer le problème = compétence qui sépare les praticiens expérimentés des débutants.

## Pourquoi c'est important

L'overfitting et l'underfitting sont les deux principaux ennemis du ML appliqué. Tant que tu ne maîtrises pas comment les reconnaître et les corriger, tu construis en aveugle. C'est le plus important après avoir des données.
